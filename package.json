{
  "name": "coral-upload-tasks",
  "version": "1.0.0",
  "description": "Module wraps a few functions from 'mongodb' module inside Tasks (from folktale/data.task), for easy function composition (using, for example, Ramda's composeK).",
  "main": "upload-tasks.js",
  "scripts": {
    "test": "./node_modules/.bin/mocha --colors --reporter spec ./**/*spec.js"
  },
  "author": "Diego Villaseñor",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-mocha": "^4.3.1",
    "gulp-watch": "^4.3.11",
    "chai": "^3.5.0",
    "mocha": "^3.2.0"
  },
  "dependencies": {
    "coral-fs-tasks": "^1.0.1",
    "coral-img-tasks": "^1.0.0",
    "data.task": "^3.1.1",
    "ramda": "^0.23.0"
  }
}
